
# ============================================================
# CourseWeave AI — Full Requirements
# ============================================================
# Install with: uv pip install -r requirements.txt --system
# Or add individually with: uv add <package>
# Note: apache-airflow and dvc are Data-Pipeline dependencies
# and should be installed in that environment separately to
# avoid conflicts with the main src/ layer.
# ============================================================


# ── Core Data & Utilities ────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
requests>=2.32.0
python-dotenv>=1.0.0


# ── Database ─────────────────────────────────────────────────
psycopg2-binary>=2.9.9


# ── GCP & Vertex AI ──────────────────────────────────────────
google-cloud-storage>=3.0.0
google-cloud-aiplatform>=1.0.0
google-genai>=1.0.0


# ── Pinecone (Hybrid Search) ─────────────────────────────────
pinecone>=3.0.0
pinecone-text>=0.9.0


# ── Embeddings & Reranking ───────────────────────────────────
sentence-transformers>=2.7.0
langchain-huggingface>=0.0.1


# ── LangChain ────────────────────────────────────────────────
langchain-core>=0.1.0
langchain-pinecone>=0.0.1
langchain-experimental>=0.0.1


# ── PDF Processing (Data Pipeline) ───────────────────────────
pymupdf>=1.23.0


# ── Web Scraping (Data Pipeline) ─────────────────────────────
beautifulsoup4>=4.12.0


# ── Bias Detection (Data Pipeline) ───────────────────────────
fairlearn>=0.9.0


# ── LLM Comparison (Evaluation) ──────────────────────────────
groq>=0.9.0
# openai>=1.0.0        # optional — uncomment if using GPT-4o mini


# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov
ruff

# ── Data Pipeline (install separately to avoid conflicts) ────
# apache-airflow>=3.0.0
# dvc>=3.0.0
# dvc-gs>=3.0.0